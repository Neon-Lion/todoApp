const config = {
    db: {
        /* Don't expose password or any sensitive info, done only for demo */
        host: "localhost",
        user: "root",
        password: "password",
        database: "node_mysql",
    }
};

module.exports = config;